import React from "react";

function RemoveButton() {
  const remove = () => {
    // call backend and remove folder
  };
  return (
    <button className="" onClick={remove}>
      Remove
    </button>
  );
}

export default RemoveButton;
